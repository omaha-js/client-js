export interface AppConstants {

	/**
	 * The public name of the application.
	 */
	app_name: string;

	/**
	 * The root URL of the application.
	 */
	app_url: string;

	/**
	 * Whether or not this application allows new accounts to be registered (without an invitation).
	 */
	allows_registration: string;

}
